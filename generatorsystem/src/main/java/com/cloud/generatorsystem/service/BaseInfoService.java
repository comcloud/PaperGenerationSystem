package com.cloud.generatorsystem.service;

import com.cloud.generatorsystem.model.Teacher;
import org.springframework.stereotype.Service;

public interface BaseInfoService {
    void saveData(Teacher teacher);
}
