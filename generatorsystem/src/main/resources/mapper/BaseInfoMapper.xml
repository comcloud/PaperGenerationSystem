<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cloud.generatorsystem.mapper">
    <insert id="saveTacherData" parameterType="com.cloud.generatorsystem.model.Teacher">
        insert into teacher value (#{teacher.name},#{teacher.department},#{teacher.tel},#{teacher.mail})
    </insert>
</mapper>